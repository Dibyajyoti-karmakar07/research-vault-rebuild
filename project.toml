[project]
name = "research-vault-rebuild"
version = "0.1.0"
description = "Learning-focused rebuild of Research Vault from first principles."
requires-python = ">=3.12"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
